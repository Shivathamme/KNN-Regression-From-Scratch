# KNN-Regression-From-Scratch
An end-to-end implementation of K-Nearest Neighbors (KNN) regression from scratch using Python and NumPy. The project covers data preprocessing, distance calculation, model evaluation using MAE and RMSE, and a comparison with Scikit-learn’s KNN regressor.
